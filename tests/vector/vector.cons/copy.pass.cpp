// #include "myUnitTest.hpp"

// TEST_CASE(Vector, CopyConstructor, SizeAndContentConsistency, {
// 	{
// 		typedef VECTOR<Aware<int> > V;

// 		V v;
// 		for (int i = 0; i < 42; ++i) {
// 			v.push_back(i);
// 		}

// 		V::size_type s = v.size();

// 		V c(v);
// 		ASSERT(c.size() == s);
// 		ASSERT(c == v);
// 	} ASSERT_AWARENESS
// })
